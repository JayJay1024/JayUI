{
  "name": "root",
  "private": true,
  "scripts": {
    "boot": "yarn && lerna bootstrap",
    "format": "prettier '**/*.{js,jsx,ts,tsx,json,md}' --write",
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.6.2",
    "prettier": "2.7.1",
    "husky": "^8.0.0"
  }
}
