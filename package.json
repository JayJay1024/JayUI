{
  "name": "root",
  "private": true,
  "scripts": {
    "boot": "yarn && lerna bootstrap",
    "format": "prettier '**/*.{js,jsx,ts,tsx,json,md}' --write"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.6.2",
    "prettier": "2.7.1"
  }
}
